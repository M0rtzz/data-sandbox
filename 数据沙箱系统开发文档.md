# 数据沙箱系统开发文档

## 1. 文档目的

本文档依据《谈判采购文件》第二部分技术要求，结合当前 `secretpad`、
`secretpad-frontend` 和 `data-sandbox-package` 的实现情况，明确后续开发范围、
人员任务、协作边界、阶段目标和验收标准。

当前系统定位为数据沙箱管理 MVP。现有 SecretPad/Kuscia 提供项目、数据表、
隐私计算任务和可视化建模基础；新增模块提供沙箱管理、资源管理、模型审批、
统一日志、系统对接和运维服务的前后端入口。后续开发目标是将管理 MVP 补齐为
能够响应采购文件技术条款并进行现场演示和验收的完整系统。

## 2. 当前总体进度

按照采购文件 100 分技术评分项进行加权估算，当前总体完成度约为 **41%**，
剩余约 **59%**。该进度为源码及当前部署配置评估结果，不包含尚未提供的第三方
测评报告、性能报告、客户案例和商务报价材料。

| 模块 | 采购权重 | 当前完成度 | 当前状态 | 主要缺口 |
| --- | ---: | ---: | --- | --- |
| 开发项目管理 | 2 | 100% | 基本完成 | 按验收字段补充开发方式、环境状态和沙箱关联展示 |
| 数据管理 | 15 | 33% | 部分完成 | 全模态数据目录、可配置抽样、自定义抽样、数据脱敏、使用控制 |
| 数据计算 | 22 | 73% | 部分完成 | JAR 上传与调试、Python 函数开发、生态库管理、API 授权控制 |
| 资源管理 | 5 | 40% | 部分完成 | 正式申请单、规格变更、延期审批、审批通过后自动拉起真实沙箱 |
| 模型审批管理 | 3 | 33% | 部分完成 | 审批参数配置、测试数据执行、运行结果查看和发布联动 |
| 日志管理 | 3 | 67% | 部分完成 | 计算算法、输入输出、数据使用链路的完整审计记录和日志上报 |
| 系统对接 | 7 | 0% | 待专项开发 | 租户开通、资源规格、自动部署、计费、可信数据流通平台对接 |
| 配套服务 | 12 | 25% | 部分完成 | 全套交付文档、第三方测评、等保密评、信创数据库适配 |
| 性能要求 | 5 | 0% | 待验证 | 并发任务、单节点沙箱实例数和批量处理吞吐量测试 |
| 案例要求 | 6 | 0% | 待准备 | 同类数据沙箱案例证明材料 |
| 源码交付 | 10 | 100% | 已具备 | 整理源码清单、许可证和构建说明 |
| 扩展节点报价 | 10 | 0% | 商务事项 | 根据最终部署拓扑形成节点报价方案 |

### 2.1 已具备的主要能力

- 项目创建、编辑、成员管理、数据授权和项目任务管理。
- 数据源、数据表、元数据、项目授权及部分使用安全配置。
- DAG 可视化编排、任务运行、日志查看、结果查看和模型管理。
- 沙箱记录创建、启停、销毁、续期、环境镜像、网络策略和快照入口。
- CPU、GPU、内存、存储资源池、租户配额、使用率和阈值告警入口。
- 模型提交、两级审核、驳回、复审、审批历史和发布状态控制。
- 操作、审计、登录、系统日志的检索、CSV 导出和留存策略。
- API 调用凭证、Webhook、失败重试、OIDC 配置和接口文档入口。
- SQLite 备份恢复、基础诊断、帮助信息和服务工单。
- Alice/Bob 本地双节点部署和合作节点注册工具。

### 2.2 当前关键限制

- 当前部署默认关闭 Kuscia 沙箱运行时，沙箱启动尚不能作为真实容器启动验收。
- GPU 库存和配额当前属于管理元数据，未与真实 GPU 发现、绑定和回收联动。
- 网络策略尚未形成可验证的容器网络隔离规则。
- 当前快照主要保存沙箱元数据，尚未覆盖工作目录、环境和运行状态。
- 资源配额由管理员直接修改，尚未形成申请、审核、执行和回收闭环。
- 模型审批尚未执行测试数据，也未展示审批测试结果。
- 通用 API、Webhook 和 OIDC 尚未实现采购文件指定的可信数据流通平台协议。
- 尚无采购要求对应的性能测试、信创适配、第三方测评和同类案例材料。

## 3. 人员任务分工

### 3.1 xzh 任务

#### X-01 前端架构与产品界面收口

- 维护数据沙箱菜单、路由、权限、错误边界和页面懒加载。
- 统一 HUSTNLP 数据沙箱品牌、页面术语、提示信息和异常反馈。
- 补齐加载、空数据、失败、无权限、重复提交和刷新恢复状态。
- 保证 Alice、Bob、中心化和分布式页面入口及交互一致。

交付物：可构建的前端工程、页面清单、路由权限表和主要页面截图。

#### X-02 开发项目与数据目录前端

- 补齐项目列表中的开发方式、环境状态、创建人和创建时间展示。
- 在项目创建流程中完成数据资源、沙箱环境和开发方式关联。
- 建设统一数据目录视图，展示元数据、提供方、有效期和使用控制策略。
- 支持文件、HTTP/API、数据库表等数据类型的筛选和详情展示。
- 提供抽样、脱敏任务配置、执行状态和结果预览页面。

交付物：项目管理页面、数据目录页面、数据治理配置页面及接口联调记录。

#### X-03 使用控制与成果导出交互

- 提供数据有效期、允许访问时间、开发结果导出和下载审批配置。
- 展示数据授权状态、策略生效范围和违规阻断原因。
- 对接后端策略校验接口，确保前端不能绕过使用控制。

交付物：使用控制配置页、导出审批交互和异常场景测试记录。

#### X-04 模型审批与测试结果前端

- 完善模型提交、审核、驳回、复审、审批记录和发布版本页面。
- 增加审批参数配置、测试数据选择、执行进度、指标和运行日志展示。
- 将模型审批状态与模型 Serving 发布入口联动，未审批模型禁止发布。

交付物：模型审批完整流程页面和端到端演示用例。

#### X-05 统一日志与审计查询前端

- 完善操作、审计、登录、系统和计算日志的组合检索。
- 增加项目、任务、算法、数据资源、用户、时间范围等查询条件。
- 完善日志详情、脱敏展示、导出和留存策略配置。
- 增加可信平台日志上报状态、失败原因和重试入口。

交付物：统一日志页面、审计查询页面和导出样例。

#### X-06 使用文档与演示材料

- 编写管理员、开发人员和审批人员操作手册。
- 编写项目创建、数据授权、沙箱申请、模型审批和日志审计演示流程。
- 整理前端功能测试用例和验收截图。

交付物：用户操作手册、演示脚本、前端测试用例和验收截图。

#### X-07 信创适配与部署工程

- 评估并适配采购方指定的国产操作系统、CPU 架构和数据库。
- 消除 SQLite 单机限制，形成可配置数据库方案和迁移工具。
- 完善中心化与分布式部署、扩展节点、升级和回滚流程。
- 输出硬件最低配置、推荐配置和部署拓扑。

交付物：适配清单、数据库迁移工具、部署包和兼容性测试报告。

### 3.2 zgz 任务

#### Z-01 真实沙箱运行时

- 制作并注册 Jupyter/Python、JAR 和 SecretFlow Kuscia AppImage。
- 打通沙箱创建、启动、停止、销毁、续期和状态同步。
- 为每个沙箱生成可访问的开发端点并完成鉴权。
- 将沙箱状态与 Kuscia Job/Task、容器和存储真实状态保持一致。
- 失败时返回明确错误，禁止将未创建容器的记录标记为运行中。

交付物：AppImage、运行时控制接口、状态同步任务和真实沙箱演示环境。

#### Z-02 资源调度与隔离

- 接入节点 CPU、GPU、内存、存储的真实发现和使用率采集。
- 实现资源预占、分配、绑定、释放和异常回收。
- 将 CPU、内存、GPU 和存储规格下发到真实运行时并验证限制生效。
- 实现内部网络、无网络和白名单策略的实际网络隔离。
- 建立资源阈值告警、沙箱异常告警和通知机制。

交付物：资源控制器、监控指标、告警规则和隔离验证报告。

#### Z-03 沙箱资源申请与审批流程

- 建立创建、延期、规格变更和回收申请单模型。
- 实现开发方提交、供数方/运营方审核、驳回、复审和审批记录。
- 审批通过后自动完成资源分配和沙箱拉起，失败时支持补偿和重试。
- 增加审批权限、并发审批和幂等控制。

交付物：资源申请 API、审批状态机、数据库迁移和流程测试。

#### Z-04 数据抽样与脱敏服务

- 实现随机、分层、整群/分块、等距等至少三种可配置抽样方法。
- 提供受控的自定义代码抽样能力和执行隔离。
- 实现掩码、替换、哈希、取整、空值/清除等脱敏方法。
- 保存抽样和脱敏策略、任务记录、数据血缘及结果数据集。
- 保证处理过程不暴露未经授权的真实数据。

交付物：数据治理 API、执行组件、策略模型、审计日志和测试数据集。

#### Z-05 JAR、SQL 与 Python 开发能力

- 实现 JAR 包上传、校验、版本管理、参数配置、调试和运行。
- 提供 SQL 编辑、执行、调试、结果预览和任务保存。
- 提供 Python 函数开发、依赖库白名单和受控生态库导入。
- 区分开发与生产运行模式，形成任务创建、运行、停止和重试闭环。

交付物：计算任务 API、运行组件、制品管理、版本管理和调试日志。

#### Z-06 模型测试执行与 API 发布

- 将模型审批单与实际模型制品、版本和项目绑定。
- 支持审批人员配置测试参数、选择测试数据并执行模型。
- 保存测试日志、输入摘要、输出摘要和评估指标。
- 实现模型发布 API、调用凭证、授权用户、IP 白名单和有效时间控制。

交付物：模型测试执行服务、审批结果模型和受控模型 API。

#### Z-07 租户、部署、计费与可信平台对接

- 实现租户开通、资源规格分配、规格变更和自动部署接口。
- 建立资源用量计量、费用计算和费用上报接口。
- 对接可信数据流通平台的数据推送、成果回传和访问控制策略。
- 实现数据开发与运算日志上报、签名验签、幂等、重试和对账。
- 提供完整 OpenAPI 文档、错误码、调用样例和联调工具。

交付物：专项适配器、接口文档、联调记录、失败补偿和对账记录。

#### Z-08 安全、备份与运维增强

- 完善 API 凭证轮换、密钥加密存储、OIDC 登录和权限映射。
- 将备份范围扩展到数据库、配置、证书、沙箱元数据和必要制品。
- 实现备份校验、恢复演练、回滚和恢复点管理。
- 完善服务健康、节点健康、任务健康、存储和证书诊断。
- 建立安全审计、敏感信息脱敏和漏洞修复流程。

交付物：安全配置、备份恢复方案、恢复演练报告和诊断工具。

#### Z-09 性能与稳定性验证

- 建设任务并发、沙箱并发和数据吞吐压测环境。
- 验证任务数大于 500，并逐步验证 1000 和 2000 任务等级。
- 验证分布式单节点并行沙箱实例数不少于 16。
- 验证结构化文本批量处理吞吐量不低于 30GB/h。
- 完成长时间稳定性、异常恢复、资源泄漏和容量边界测试。

交付物：压测工具、测试数据、性能报告、瓶颈分析和优化记录。

## 4. 协作与版本管理

### 4.1 开发协作边界

- 前后端以 OpenAPI 为契约。后端接口变更须同步接口文档和错误码，前端不得依赖
  未声明字段。
- xzh 负责页面交互、前端状态、权限入口和验收演示；zgz 负责后端状态机、运行时、
  数据一致性、安全边界和基础设施联动。
- 跨模块功能以端到端用例为完成标准，不能以页面存在、数据库有记录或接口返回
  成功作为单独完成依据。
- 每个功能提交必须包含必要的单元测试或集成测试；涉及运行时、资源、数据安全和
  恢复的功能必须提供可重复验证步骤。
- 两个子仓库分别开发和提交，根仓库只更新已验证的 submodule commit，不直接保存
  子仓库未提交改动。

### 4.2 代码同步要求

- xzh 和 zgz 使用各自的系统用户、工作目录和 Git 工作副本进行开发，禁止两人直接
  修改同一个工作目录。
- 两人之间只通过 Git `pull` 和 `push` 同步代码，不通过 `scp`、`rsync`、共享目录
  覆盖或手工复制源文件，避免未提交修改和生成文件相互污染。
- xzh 使用 `develop/xzh` 分支，zgz 使用 `develop/zgz` 分支；功能完成后通过合并
  请求或明确的 commit 合并，不直接向 `master` 强制推送。
- 每次开始开发前先拉取远程最新代码；推送前再次拉取并处理冲突，确保本地提交基于
  最新远程版本。
- 禁止对共享分支执行强制推送。发生冲突时必须在个人工作目录中解决、测试并形成
  新提交，不能通过覆盖其他人的文件解决冲突。
- 前后端修改分别提交到 `secretpad` 和 `secretpad-frontend`。根仓库只记录已经
  合并并验证的 submodule commit SHA。
- 提交信息应说明模块、变更内容、数据库迁移、配置变更和兼容性影响；未提交或未推送
  的代码不进入统一构建。
- 本地运行日志、数据库、证书、密钥、镜像产物、运行时目录和个人配置不得提交到
  Git。

### 4.3 统一构建与部署要求

- **xzh 是共享系统唯一的构建和部署执行人。** zgz 不在共享 Alice/Bob 环境中执行
  `build.sh`、`install.sh`、容器替换、数据库迁移、恢复或回滚操作。
- zgz 可以在个人工作目录执行单元测试、静态检查和不影响共享环境的本地验证，但正式
  JAR、前端静态资源、Docker 镜像和部署包均由 xzh 从已推送代码统一生成。
- zgz 的集成测试统一使用其个人代码副本中的 `data-sandbox-package/develop.sh`。
  该脚本只能创建 zgz 私有的 Kuscia、SecretPad、数据库、证书、网络、端口和运行
  目录，不得读取或挂载任何 xzh 路径及共享 Alice/Bob 数据。
- zgz 运行 `develop.sh up` 前必须提交并推送前后端代码；脚本验证两个仓库均处于
  `develop/zgz`、工作区无未提交文件并与远程 upstream 完全一致后才允许构建。
- zgz 可使用 `develop.sh status`、`logs`、`restart` 和 `down` 管理个人测试实例。
  `down` 只停止带有当前用户和当前工作区所有权标签的容器，并保留个人测试数据。
- xzh 构建前必须确认两个子仓库工作区干净、目标分支已拉取、待发布提交已合并，且
  根仓库记录的 submodule SHA 与计划发布版本一致。
- 构建期间冻结待发布 commit。发现问题后以新 commit 修复并重新构建，禁止直接修改
  已构建目录、JAR 内文件或容器内文件。
- 所有共享环境安装、升级、数据库迁移、配置切换、服务重启和回滚均由 xzh 执行，
  其他人员只能查看状态和日志，不得同时操作部署。
- 部署前必须保留当前数据库、配置和容器回滚点；部署失败时按已验证的回滚流程恢复，
  禁止临时覆盖运行数据。
- 开发环境、测试环境和共享演示环境使用独立端口、容器名和数据目录，禁止多个版本
  共用同一数据库或运行时目录。

### 4.4 版本锁定与发布记录

每次统一构建和部署必须记录以下信息：

- 根仓库 commit SHA。
- `secretpad` submodule commit SHA。
- `secretpad-frontend` submodule commit SHA。
- 构建时间、执行人和构建命令。
- JAR 校验和、Docker 镜像标签及镜像摘要。
- 数据库迁移版本、部署配置版本和必要的配置差异。
- 部署目标、部署结果、验证结果和回滚点。

共享系统页面中展示的版本必须能够对应到上述发布记录。未形成发布记录的构建不得
部署到共享演示或验收环境。

### 4.5 统一发布流程

1. xzh 和 zgz 完成个人分支开发、测试、提交并推送远程仓库。
2. 代码评审通过后合并目标 commit，处理前后端接口和数据库迁移依赖。
3. xzh 在独立构建目录拉取根仓库，并同步到指定的两个 submodule commit。
4. xzh 执行前端检查、后端测试和部署包构建，生成唯一版本的发布产物。
5. xzh 记录版本信息并备份共享系统当前数据库、配置和容器状态。
6. xzh 部署新版本，执行登录、项目、数据、沙箱、审批、日志和节点联通冒烟测试。
7. 验证通过后更新根仓库 submodule 指针和发布记录；验证失败则回滚并以新 commit
   修复，禁止在服务器上直接改代码。

## 5. 阶段任务与完成标准

| 阶段 | 目标 | 当前状态 | 完成标准 |
| --- | --- | --- | --- |
| M0 管理 MVP | 六个管理模块及前后端入口 | 基本完成 | 页面、API、数据库迁移和部署包可用 |
| M1 真实沙箱 | 运行时、资源隔离、网络策略和状态同步 | 待开发 | 可创建并进入真实沙箱，资源限制和状态一致 |
| M2 数据治理与开发 | 抽样、脱敏、JAR、SQL、Python 和使用控制 | 待开发 | 采购要求方法可配置、可执行、可审计 |
| M3 审批与系统对接 | 资源审批、模型测试、租户计费和可信平台 | 待开发 | 关键业务形成端到端闭环并通过联调 |
| M4 产品化验收 | 信创、文档、测评、性能、案例和交付 | 待准备 | 必要条款逐项有实现、演示或证明材料 |

## 6. 近期优先级

1. 先完成真实 Jupyter/Python 沙箱和资源隔离，消除“状态运行但无实际容器”的问题。
2. 完成资源申请审批闭环，使审批通过能够自动分配资源并拉起沙箱。
3. 完成数据抽样、脱敏和使用控制，覆盖采购文件中的必要数据治理能力。
4. 完成模型测试执行和受控 API 发布。
5. 根据可信数据流通平台实际协议完成专项对接，不以通用 Webhook 代替验收接口。
6. 最后开展信创适配、性能压测、第三方测评和交付材料整理。

## 7. 验收判定原则

- “已完成”必须同时具备前端操作、后端接口、持久化、权限、异常处理和测试证据。
- 沙箱启动必须存在真实运行实例，并能够通过平台进入开发环境。
- CPU、GPU、内存、存储和网络策略必须在运行实例上实际生效。
- 数据抽样和脱敏必须产生可追溯的新数据集，且全过程形成审计记录。
- 审批必须能控制后续资源分配或模型发布，不接受仅修改审批状态的实现。
- 系统对接必须使用采购方确认的协议完成实际联调。
- 性能指标必须以可复现的测试脚本、环境参数和正式报告为依据。
- 所有采购文件中的必要功能均须提供实现、现场演示或有效证明材料。
