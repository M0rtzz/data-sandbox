# xzh 功能测试文档

## 1. 目的和范围

本文档用于测试 `develop/xzh` 分支中由 xzh 负责的前端交互、页面状态、前后端接口
联调入口、开发测试脚本和验收演示流程。测试对象是个人隔离开发环境，不得使用共享
Alice/Bob 容器、数据库、证书、端口或快照目录。

本文档覆盖 X-01 至 X-07 中当前能够实际验证的内容。真实沙箱运行时、资源强制隔离、
抽样脱敏执行、模型测试执行及采购方专项对接由相应后端运行时完成前才能验收，本文
明确记录为“依赖后端，不通过本轮测试冒充完成”。

### 1.1 xzh 任务全量对照

| 任务 | 可测试范围 | 当前判定 | 测试章节 |
| --- | --- | --- | --- |
| X-01 前端架构与产品界面 | 数据沙箱菜单、路由、品牌、懒加载、加载/失败/重试/刷新状态 | 可验证 | 第 3 节 |
| X-02 开发项目与数据目录 | 项目元数据展示、已有数据源/数据表浏览、文件/HTTP/数据库类型筛选和详情 | 部分可验证；项目-沙箱关联、有效期、抽样和脱敏执行待后端 | 第 4 节 |
| X-03 使用控制与成果导出 | 已有项目授权、字段配置入口及边界提示 | 部分可验证；时效控制、外发审批和策略强制校验待后端 | 第 5 节 |
| X-04 模型审批与测试结果 | 提交、两级审批、驳回、复审、历史及发布控制 | 部分可验证；测试数据、真实执行、指标和 Serving 联动待后端 | 第 6 节 |
| X-05 统一日志与审计查询 | 类型/级别/用户/关键字/时间组合查询、CSV 导出、留存策略 | 部分可验证；项目/任务/算法/血缘日志及可信平台重试待后端 | 第 7 节 |
| X-06 使用文档与演示材料 | 操作步骤、帮助、诊断、工单、截图和测试记录 | 可验证 | 第 8 节和第 10 节 |
| X-07 信创适配与部署工程 | 个人隔离开发栈、构建来源保护、容器和端口隔离 | 部分可验证；国产软硬件适配、可配置数据库、迁移/升级/回滚和兼容性报告尚未验收 | 第 9 节 |

表中“可验证”表示可在当前 `develop/xzh` 私有环境重复执行；“部分可验证”仅表示表内
所列的已实现交互或接口可测，未列出的后端闭环必须等待相应服务实现后再补充测试证据。

## 2. 测试前准备

### 2.1 分支和工作目录

确认两个子仓库均位于 `develop/xzh`：

```bash
git -C secretpad branch --show-current
git -C secretpad-frontend branch --show-current
```

预期输出均为 `develop/xzh`。日常测试允许工作区有未提交修改；测试通过后再提交和
推送。发布候选验证才使用 `--pushed-only`。

### 2.2 启动个人测试环境

第一次启动或前端、后端代码有修改时执行：

```bash
cd /data/xzh/Workspaces/Misc/data-sandbox/data-sandbox-package
./develop.sh up --name xzh --branch develop/xzh
```

镜像已构建且只需重启私有环境时执行：

```bash
./develop.sh up --name xzh --branch develop/xzh --skip-build
```

首次启动时设置个人测试管理员密码。默认用户名为 `devadmin`，密码不写入本文档，
由启动人员保管。开发环境使用以下私有端口：

| 服务 | 端口 |
| --- | ---: |
| 控制台 | 19088 |
| Kuscia 网关 | 19080 |
| Kuscia HTTP API | 19082 |
| Kuscia gRPC API | 19083 |
| Kuscia 内部服务 | 19081 |
| Kuscia 指标 | 19084 |

浏览器访问地址：

```text
http://222.20.99.38:19088/login
```

登录后进入：

```text
http://222.20.99.38:19088/edge?ownerId=dev-xzh&tab=workbench
```

运行状态和日志：

```bash
./develop.sh status --name xzh --branch develop/xzh
./develop.sh logs --name xzh --branch develop/xzh --component secretpad
./develop.sh logs --name xzh --branch develop/xzh --component kuscia
```

重复测试请使用唯一名称，例如 `xzh-sbx-20260817-01`、`xzh-model-20260817-01`，避免
与此前测试记录混淆。结束后执行 `./develop.sh down --name xzh --branch develop/xzh`；
该操作仅停止当前用户私有容器，保留个人测试数据。

### 2.3 统一测试输入数据

以下数据作为一轮完整测试的固定输入。再次测试时只需将名称末尾的 `01` 改为 `02`、
`03` 等新序号。表单中由系统生成的节点 ID、项目 ID、认证码、证书和凭证密钥必须
使用页面实际返回值，不能用示例字符串代替。

本轮所有“创建、提交或保存”操作按下表执行，不遗漏只需点击、没有输入字段的操作：

| 序号 | 对象或操作 | 输入位置 | 是否可在纯测试环境执行 |
| --- | --- | --- | --- |
| 1 | 本方计算节点 | 第 2.3.1 节 | 可执行 |
| 2 | 第二套独立测试节点 | 第 2.3.2 节 | 可执行 |
| 3 | 双向合作节点路由 | 第 2.3.2 节 | 可执行 |
| 4 | 双节点协作项目 | 第 2.3.3 节 | 节点路由 `READY` 后可执行 |
| 5 | 数据源 | 第 2.3.4 节 | 本地数据可执行；外部数据源须有纯测试服务 |
| 6 | 两方数据表 | 第 2.3.4 节 | 可执行 |
| 7 | 项目数据授权 | 第 2.3.5 节 | 可执行 |
| 8 | 环境镜像记录 | 第 2.3.6 节 | 可执行 |
| 9 | 沙箱记录 | 第 2.3.6 节 | 可执行；真实启动暂不可验收 |
| 10 | 节点资源配额 | 第 2.3.6 节 | 可执行；真实强制隔离暂不可验收 |
| 11 | 模型通过/发布记录 | 第 2.3.7 节 | 可执行 |
| 12 | 模型驳回/复审记录 | 第 2.3.7 节 | 可执行 |
| 13 | API 调用凭证 | 第 2.3.8 节 | 可执行 |
| 14 | Webhook 与失败重试 | 第 2.3.8 节 | 可执行 |
| 15 | OIDC 配置和失败提示 | 第 2.3.8 节 | 可执行；真实 SSO 暂不可验收 |
| 16 | 日志留存策略 | 第 2.3.9 节 | 可执行 |
| 17 | 数据库备份记录 | 第 2.3.9 节 | 可执行 |
| 18 | 服务支持工单 | 第 2.3.9 节 | 可执行 |

#### 2.3.1 创建本方节点

在提供“注册节点”的中心化节点管理页面中使用：

| 字段 | 测试值 |
| --- | --- |
| 节点名称 | `xzh-node-20260817-01` |
| 节点能力 | 页面固定为 `MPC`，无需填写 |

预期：注册成功后记录页面生成的节点 ID，节点名称不超过 32 个字符，只包含中英文、
数字、下划线或中划线。当前 xzh 私有环境为自治模式时，不重复注册本方主节点，直接
使用已有节点 `dev-xzh`。

自治模式“我的节点 -> 新增计算节点”另使用：

| 字段 | 测试值 |
| --- | --- |
| 节点名称 | `xzh-calc-20260817-01` |

预期：新增成功并生成新的计算节点 ID，列表状态最终进入 `READY`。这个附属计算节点
只用于验证新增节点页面，不代替第 2.3.2 节的独立机构测试节点；多机构项目仍使用
`dev-xzh` 与 `dev-xzh-peer`。

#### 2.3.2 创建并添加第二个纯测试节点

不得使用共享或上线环境中的 Alice/Bob 节点。创建多方项目需要至少两个状态为
`READY` 的节点，因此在 xzh 自己的工作区再启动一个完全隔离的 `xzh-peer` 测试实例。
不要使用 `sudo`，执行：

```bash
cd /data/xzh/Workspaces/Misc/data-sandbox/data-sandbox-package
./develop.sh up \
  --name xzh-peer \
  --branch develop/xzh \
  --port 19188 \
  --gateway-port 19180 \
  --api-http-port 19182 \
  --api-grpc-port 19183 \
  --internal-port 19181 \
  --metrics-port 19184 \
  --admin-user peeradmin
```

首次启动会要求输入并确认 `peeradmin` 的独立测试密码。该实例自动使用：

| 项目 | 测试值 |
| --- | --- |
| 节点 ID | `dev-xzh-peer` |
| 机构名称 | `DataSandbox-xzh-peer` |
| 控制台 | `http://222.20.99.38:19188/login` |
| 网关 | `https://222.20.99.38:19180` |
| 运行目录 | `.dev-runtime/xzh-peer` |
| Kuscia 容器 | `data-sandbox-dev-xzh-peer-kuscia` |
| 控制台容器 | `data-sandbox-dev-xzh-peer-secretpad` |

确认第二个测试实例运行：

```bash
./develop.sh status \
  --name xzh-peer \
  --branch develop/xzh \
  --port 19188 \
  --gateway-port 19180 \
  --api-http-port 19182 \
  --api-grpc-port 19183 \
  --internal-port 19181 \
  --metrics-port 19184
```

测试结束后停止第二个测试实例：

```bash
./develop.sh down --name xzh-peer --branch develop/xzh
```

`down` 只停止 `xzh-peer` 的私有容器并保留其测试数据，不会操作 xzh 主测试实例或
任何共享/上线环境。

节点公钥不需要手工另建。`develop.sh` 初始化每个私有 Kuscia 节点时会自动生成节点
私钥、证书和 Domain 资源；“节点公钥”表单实际使用 Domain 的 `spec.cert`，其内容是
Base64 编码的 PEM 证书。推荐通过页面获取：登录 `xzh-peer`，进入“我的节点”，选择
`dev-xzh-peer`，点击“复制公钥”。也可以执行以下备用命令获取同一个值：

```bash
docker exec data-sandbox-dev-xzh-peer-kuscia sh -lc \
  "bin/kuscia kubectl --kubeconfig /home/kuscia/etc/kuscia.kubeconfig \
  get domain dev-xzh-peer -o jsonpath='{.spec.cert}'"
```

xzh 主测试节点对应命令为：

```bash
docker exec data-sandbox-dev-xzh-kuscia sh -lc \
  "bin/kuscia kubectl --kubeconfig /home/kuscia/etc/kuscia.kubeconfig \
  get domain dev-xzh -o jsonpath='{.spec.cert}'"
```

输出应以 `LS0tLS1CRUdJTi...` 开头。粘贴“节点公钥”时使用完整单行原值，不要 Base64
解码、不要只复制开头或结尾，也不要复制私钥。需要检查证书而不改变原值时可执行：

```bash
docker exec data-sandbox-dev-xzh-peer-kuscia sh -lc \
  "bin/kuscia kubectl --kubeconfig /home/kuscia/etc/kuscia.kubeconfig \
  get domain dev-xzh-peer -o jsonpath='{.spec.cert}'" \
  | base64 -d \
  | openssl x509 -noout -subject -issuer -dates -fingerprint -sha256
```

当前前端解析节点认证码后不会自动填充“节点公钥”，必须单独复制。添加
`dev-xzh-peer` 时访问
`http://222.20.99.38:19188/my-node?ownerId=zdlmiizd`，选择 `dev-xzh-peer` 并点击
“复制公钥”，再把完整单行内容粘贴到 xzh 的“节点公钥”字段。反向添加 `dev-xzh` 时，
访问 `http://222.20.99.38:19088/my-node?ownerId=kkeaaeve` 复制 `dev-xzh` 的公钥。
通讯地址发生变化后应重新复制认证码，不能继续使用旧认证码与旧地址组合。

然后用 `peeradmin` 登录 `xzh-peer` 控制台，在“我的节点”页面生成并复制当次节点
认证码。回到 xzh 主测试控制台，在“合作节点 -> 添加合作节点”中粘贴完整认证码并
点击“识别解析”。

首次添加前先分别修正“我的节点 -> 通讯地址”（输入时不带 `https://`）：

| 控制台 | 本方节点 | 通讯地址 |
| --- | --- | --- |
| `http://222.20.99.38:19088` | `dev-xzh` | `222.20.99.38:19080` |
| `http://222.20.99.38:19188` | `dev-xzh-peer` | `222.20.99.38:19180` |

当前测试数据库初始值可能显示为 `127.0.0.1:28080`，该地址不能用于两个隔离容器之间
通信。必须修改并保存正确地址后重新复制节点认证码，因为通讯地址变化会使认证码变化。

在 xzh 主控制台添加合作节点时，字段方向固定为：

```text
本方节点 dev-xzh（19080）  --->  合作节点 dev-xzh-peer（19180）
```

认证码解析后按下表核对或填写：

| 字段 | xzh 侧测试值或来源 |
| --- | --- |
| 节点认证码 | `xzh-peer` 页面当次生成的完整认证码，不换行、不删字符 |
| 所属机构 | 由认证码自动解析，应为 `DataSandbox-xzh-peer` |
| 所属机构 ID | 由认证码自动解析，以页面实际值为准 |
| 管控节点 ID | `dev-xzh-peer` |
| 计算节点名 | `dev-xzh-peer`，以认证码解析结果为准 |
| 计算节点 ID | `dev-xzh-peer` |
| 协议 | `https://` |
| 合作节点通讯地址 | `222.20.99.38:19180` |
| 节点公钥 | 从 `http://222.20.99.38:19188/my-node?ownerId=zdlmiizd` 的 `dev-xzh-peer` 节点单独复制 |
| 本方节点 | `dev-xzh` |
| 本方协议 | `https://` |
| 本方节点通讯地址 | `222.20.99.38:19080` |

两个测试实例位于不同的私有 Docker 网络，所以认证码若解析出
`data-sandbox-dev-xzh-peer-kuscia:1080`，应将“合作节点通讯地址”改为宿主机可达的
`222.20.99.38:19180`。随后在 `xzh-peer` 侧反向添加 `dev-xzh`：使用 xzh 当次生成的
认证码，将合作节点通讯地址填写为 `222.20.99.38:19080`，本方节点选择
`dev-xzh-peer`，本方通讯地址填写 `222.20.99.38:19180`。两边完成授权且状态均为
`READY` 后再创建项目；只有页面记录、没有真实路由的虚构节点不能用于项目测试。

#### 2.3.3 创建项目

在“我的项目 -> 新建项目”填写：

| 字段 | 测试值 |
| --- | --- |
| 项目名称 | `xzh-project-20260817-01` |
| 项目描述 | `客户流失联邦学习测试` |
| 计算功能 | `模型训练-联合建模` |
| 计算模式 | `管道模式（MPC）` |
| 本方节点 | `dev-xzh` |
| 受邀节点 | `dev-xzh-peer` |

当前私有测试实例是点对点模式，按上表填写；本方节点与受邀节点必须已经双向授权，
且路由状态为 `Succeeded/READY`。若以后在中心化测试实例执行同一用例，训练流模板
选择“空白训练流”，参与节点选择 `dev-xzh` 和 `dev-xzh-peer`。创建成功后从项目详情
或地址栏复制真实项目 ID，后续模型提交和数据授权均使用该真实 ID。若创建按钮不可用，
先检查本方节点、受邀节点和节点路由状态。

点对点项目提交后，再登录 `http://222.20.99.38:19188`，由 `peeradmin` 在消息中心
同意项目创建邀请；审批备注如页面要求填写，使用 `同意纯测试项目创建`。回到 xzh
主测试控制台刷新项目列表，只有项目状态进入可用状态后才继续上传和授权数据。

#### 2.3.4 创建数据源并上传测试数据表

纯测试环境优先使用系统自带的“本地数据源/default-data-source”上传 CSV，不连接
上线环境数据库、OSS 或 ODPS，也不填写任何生产账号、AccessKey 或密码。

如果需要验证“创建数据源”表单，可连接一套由测试人员单独提供、且与上线环境完全
隔离的 MySQL。表单输入统一使用：

| 字段 | 纯测试值 |
| --- | --- |
| 数据源类型 | `MYSQL` |
| 显示名称 | `xzh-mysql-source-20260817-01` |
| endpoint 名称 | `<纯测试MySQL主机名或IP>:3306` |
| user | `datasandbox_test` |
| password | 纯测试数据库临时密码，不写入 Git |
| database | `data_sandbox_test` |
| 节点连接配置 | `dev-xzh` |

数据库中用于创建数据表资源的测试表名为 `customer_profile`。只有测试 MySQL 已真实
启动、上述用户能够访问 `data_sandbox_test.customer_profile` 时才提交；否则本项记录
为“未执行：缺少纯测试 MySQL”，不能填写上线环境地址，也不能把表单校验通过记为
数据源创建成功。OSS 和 ODPS 同理，必须使用单独测试租户，当前纯本地测试不执行。

创建 MySQL 数据表资源时填写：

| 字段 | 测试值 |
| --- | --- |
| 所属数据源 | `xzh-mysql-source-20260817-01` |
| 数据表地址 | `customer_profile` |
| 数据表名称 | `xzh-mysql-customers-20260817-01` |
| 所属节点 | `dev-xzh` |
| 描述 | `纯测试MySQL客户信息表` |
| 空缺值 | `""` |
| 字段结构 | `customer_id:string`、`age:int`、`city:string`、`income:float` |

不具备测试 MySQL 时，直接执行下面的本地 CSV 用例，仍可完成项目、数据目录和授权
页面测试。

将下面两个代码块分别保存为 UTF-8 编码的 `xzh_customers_20260817_01.csv` 和
`xzh_peer_customers_20260817_01.csv`。第一份数据上传到 `dev-xzh`，第二份数据登录
纯测试控制台后上传到 `dev-xzh-peer`。

`xzh_customers_20260817_01.csv`：

```csv
customer_id,age,city,income
C001,28,Wuhan,8500.50
C002,35,Changsha,12000.00
C003,42,Wuhan,15600.75
C004,31,Nanchang,9800.00
C005,26,Hefei,7600.25
```

`xzh_peer_customers_20260817_01.csv`：

```csv
customer_id,contract_months,monthly_fee,churn
C001,12,89.00,0
C002,3,129.50,1
C003,24,79.00,0
C004,6,109.00,1
C006,18,69.50,0
```

上传表单使用：

| 字段 | xzh 主测试节点 | xzh-peer 测试节点 |
| --- | --- | --- |
| 所属数据源 | `本地数据源/default-data-source` | `本地数据源/default-data-source` |
| 上传文件 | `xzh_customers_20260817_01.csv` | `xzh_peer_customers_20260817_01.csv` |
| 数据表名称 | `xzh-customers-20260817-01` | `xzh-peer-customers-20260817-01` |
| 描述 | `客户基础信息测试数据` | `客户合约标签测试数据` |
| 空缺值 | `""` | `""` |
| 主键/关联字段 | `customer_id` | `customer_id` |
| 所属节点 | `dev-xzh` | `dev-xzh-peer` |

字段类型依次配置为 xzh 主测试节点侧 `string/int/string/float`，xzh-peer 侧
`string/int/float/int`。预期两个表通过 `customer_id` 有 `C001` 至 `C004` 四条交集，
可用于后续隐私集合求交或联邦建模联调。测试数据不包含真实个人信息。

#### 2.3.5 数据授权

| 字段 | 测试值 |
| --- | --- |
| 数据资源 | `xzh-customers-20260817-01` |
| 授权项目 | 上一步创建的 `xzh-project-20260817-01` |
| 允许字段 | `customer_id`、`age`、`city`、`income` |
| 联合分析关联字段 | `customer_id` |

保存后重新打开授权管理，确认项目和字段仍然可见。数据有效期、访问时段和成果外发
审批目前没有真实策略闭环，不填写虚构结果。

#### 2.3.6 环境镜像、沙箱和资源配额

环境镜像记录使用：

| 字段 | 测试值 |
| --- | --- |
| 镜像名称 | `xzh-jupyter-20260817-01` |
| OCI 镜像地址 | `jupyter/scipy-notebook:python-3.11` |
| Kuscia AppImage | 留空，直到后端实际注册同名 AppImage |
| 启用 | 是 |

创建沙箱使用：

| 字段 | 测试值 |
| --- | --- |
| 沙箱名称 | `xzh-sandbox-20260817-01` |
| 所属节点 | `dev-xzh` |
| 环境镜像 | `xzh-jupyter-20260817-01` |
| CPU | `2` 核 |
| 内存 | `4` GB |
| GPU | `0` |
| 存储 | `20` GB |
| 有效期 | `7` 天 |
| 网络策略 | `仅平台内网（INTERNAL_ONLY）` |

在“资源管理 -> 调整节点资源配额”使用：

| 字段 | 测试值 |
| --- | --- |
| 节点 ID | `dev-xzh` |
| CPU | `8` 核 |
| 内存 | `32` GB |
| GPU（A100） | `0` |
| 存储 | `200` GB |

当前没有注册真实 Jupyter/Python AppImage，因此可以测试镜像记录、沙箱记录、配额、
期限和错误提示，但点击“启动”应明确失败，不能把记录创建成功写成真实沙箱启动成功。

#### 2.3.7 模型审批

第一条记录用于通过和发布流程：

| 字段 | 测试值 |
| --- | --- |
| 模型名称 | `xzh-churn-model-20260817-01` |
| 数据沙箱模型 ID | `model-xzh-churn-20260817-01` |
| 项目 ID | 第 2.3.3 节页面生成的真实项目 ID |
| 提交说明 | `客户流失模型审批测试` |
| 第一次审批 | `模型审核通过，进入资源审核` |
| 第一次审批意见 | `模型结构和输入字段检查通过` |
| 第二次审批 | `资源审核通过` |
| 第二次审批意见 | `测试资源配额检查通过` |

第二条记录用于驳回和复审流程：

| 字段 | 测试值 |
| --- | --- |
| 模型名称 | `xzh-churn-model-reject-20260817-01` |
| 数据沙箱模型 ID | `model-xzh-reject-20260817-01` |
| 项目 ID | 同一真实项目 ID |
| 提交说明 | `模型驳回和复审流程测试` |
| 审批结果 | `驳回` |
| 审批意见 | `缺少测试指标请补充后复审` |

#### 2.3.8 API 凭证、Webhook 和 OIDC

API 调用凭证：

| 字段 | 测试值 |
| --- | --- |
| 凭证名称 | `xzh-api-client-20260817-01` |
| 权限范围 | `sandbox:read sandbox:write` |

创建后立即把页面只显示一次的 Client ID 和 Client Secret 保存到个人测试记录，禁止
提交到 Git 或写入本文档。

Webhook 失败与重试用例：

| 字段 | 测试值 |
| --- | --- |
| 名称 | `xzh-webhook-failure-20260817-01` |
| 回调地址 | `http://127.0.0.1:9/data-sandbox-test` |
| 事件 | `sandbox.created,model.published` |
| 签名密钥 | `xzh-webhook-test-20260817-01` |
| 启用 | 是 |

端口 `9` 故意没有回调服务，用于产生 `FAILED` 投递记录并验证“失败原因”和“重试”
按钮。成功回调用例必须换成测试人员实际控制且后端容器可访问的 HTTP 接收地址。

OIDC 保存与失败提示用例：

| 字段 | 测试值 |
| --- | --- |
| Issuer URL | `https://idp.example.invalid/realms/data-sandbox` |
| Client ID | `xzh-data-sandbox-test` |
| Client Secret | `xzh-oidc-test-only` |
| Scopes | `openid profile email` |
| 启用配置 | 否 |

`.invalid` 是保留的无效域名，该数据用于验证配置保存和 Discovery 失败提示。OIDC
成功测试必须使用采购方提供的真实测试 IdP，当前不能据此判定 SSO 已联通。

#### 2.3.9 日志留存、备份、运维工单与检索

在“统一日志 -> 留存策略”逐条保存：

| 日志类型 | 留存天数 |
| --- | ---: |
| `OPERATION` | `180` |
| `AUDIT` | `365` |
| `LOGIN` | `180` |
| `SYSTEM` | `90` |

每次保存后刷新并确认更新人和留存天数仍然存在。随后进入“运维服务 -> 立即备份”；
该操作没有表单输入。预期生成一条新的备份 ID，状态为 `COMPLETED`，大小大于 `0`，
且创建时间为当前测试时间。“恢复暂存”会产生待部署脚本处理的恢复记录，不在正在
运行的测试实例中直接覆盖数据库。

工单输入：

| 字段 | 测试值 |
| --- | --- |
| 标题 | `xzh沙箱启动失败测试工单20260817-01` |
| 分类 | `TECHNICAL` |
| 优先级 | `NORMAL` |
| 问题描述 | `测试环境未注册Jupyter AppImage沙箱启动返回明确错误请核查` |

完成上述创建操作后，在统一日志中依次使用关键字
`xzh-sandbox-20260817-01`、`model-xzh-churn-20260817-01` 和操作人 `devadmin`
检索；日期范围选择测试当天，日志类型分别选择操作、审计、登录和系统日志。

## 3. X-01 前端架构与产品界面

### 3.1 菜单、路由与懒加载

1. 登录后检查左侧菜单包含“沙箱管理、资源管理、模型审批、统一日志、系统对接、
   运维服务”。
2. 分别点击六个菜单，或直接访问：

```text
/edge?ownerId=dev-xzh&tab=sandbox-manager
/edge?ownerId=dev-xzh&tab=resource-manager
/edge?ownerId=dev-xzh&tab=model-approval
/edge?ownerId=dev-xzh&tab=unified-log
/edge?ownerId=dev-xzh&tab=integration-manager
/edge?ownerId=dev-xzh&tab=operation-center
```

预期：每个页面独立加载；单个页面接口失败时只显示“页面数据加载失败”和“重试”，
不导致工作台或其他菜单白屏。页面标题使用“数据沙箱”术语，不展示旧品牌的官网、
社区或教程入口。

### 3.2 异常和刷新恢复

1. 在任一管理页面点击“刷新”，确认加载状态结束后表格或卡片恢复可操作。
2. 临时停止私有 SecretPad 容器，刷新页面，再启动容器并点击“重试”。

预期：失败时展示明确错误和重试按钮；服务恢复后页面能够重新加载。不得自动打开
新浏览器标签页。

## 4. X-02 开发项目与数据目录

### 4.1 项目展示

1. 打开“我的项目”。
2. 创建一个空白项目，填写唯一项目名称和描述；或选择已有项目。
3. 检查项目卡片。

预期：项目卡片展示计算/开发方式、项目审批或环境状态、创建人、创建时间、参与节点、
训练流和任务数量；“进入项目”能够进入对应 DAG 页面。

### 4.2 数据资源目录

1. 打开“数据源管理”，创建或检查一个文件、HTTP 或数据库数据源。
2. 打开“数据管理”。
3. 检查数据资源列表，点击一条资源查看详情。
4. 使用数据源类型、节点名称和资源名称筛选。

预期：列表展示数据资源名称、数据格式、数据源类型、数据源名称、所属节点、授权项目
和可用状态；详情展示数据源、数据地址、字段结构、空缺值和描述。

### 4.3 当前限制

抽样、脱敏任务配置、执行状态、结果预览、数据有效期和统一数据目录元数据尚依赖数据
治理后端。当前不得将“数据管理”页面的资源浏览和项目授权视为抽样或脱敏已验收。

## 5. X-03 数据授权、使用控制与成果导出

### 5.1 字段授权

1. 在“数据管理”中选择一条资源，点击“授权管理”。
2. 选择一个项目，配置允许使用的字段及安全配置。
3. 保存后返回列表，确认“已授权项目”数量变化。
4. 再次进入授权管理，修改字段配置或取消授权。

预期：授权仅能选择当前节点可参与的项目；保存后项目授权列表可见；字段配置入口有
“当前仅针对 SCQL 联合分析组件生效”的边界提示。

### 5.2 当前限制

数据有效期、允许访问时间、结果外发审批、下载阻断原因和后端策略强制校验尚未形成
可执行闭环。因此这些项本轮标记为“依赖后端，不通过”，不得以页面存在或手工限制
替代验收。

## 6. X-04 模型审批页面

### 6.1 提交、审核、驳回、复审和发布

1. 打开“模型审批”，点击“提交模型”。
2. 填写模型名称、模型 ID、项目 ID 和提交说明，提交。
3. 在新记录中点击“审批”，选择“通过”并填写审批意见。
4. 再次审批通过资源审核，确认状态变为“已批准”。
5. 点击“发布”，确认状态变为“已发布”。
6. 新建另一条模型并选择“驳回”，再点击“提交复审”。
7. 点击“审批记录”，检查状态迁移、操作人、时间和审批意见。

预期：状态顺序为“待模型审核 -> 待资源审核 -> 已批准 -> 已发布”；只有已驳回模型
可以复审，只有已批准模型可以发布；审批记录完整可追溯。

### 6.2 当前限制

测试数据选择、模型实际执行、进度、输入输出摘要、指标和运行日志依赖模型测试执行
服务。当前页面不能将模型审批状态误认为模型已在测试数据上执行成功。

## 7. X-05 统一日志与审计查询

### 7.1 组合检索和导出

1. 依次执行沙箱创建、配额修改、模型提交等操作，生成测试日志。
2. 打开“统一日志”。
3. 分别使用日志类型、级别、操作人、关键字和日期范围筛选；可组合两个以上条件。
4. 点击“导出 CSV”，用表格工具打开下载文件。
5. 点击“留存策略”，修改一种日志类型的留存天数并保存。

预期：列表包含操作、审计、登录、系统日志；组合筛选后的结果与导出 CSV 一致；CSV
使用 UTF-8 BOM，在常见表格工具中中文不乱码；留存策略保存后重新刷新可见。

### 7.2 当前限制

计算算法、输入输出、数据使用血缘及可信平台上报状态依赖计算和专项对接后端。当前
统一日志只验收已接入管理操作的审计记录。

## 8. X-06 使用、帮助与演示材料

### 8.1 运维帮助和工单

1. 打开“运维服务”。
2. 检查运行中沙箱、待审批模型、未恢复告警和失败回调统计卡片。
3. 阅读“帮助文档”中的快速入门、网络策略、模型审批和备份恢复说明。
4. 点击“一键诊断”，检查数据库、快照目录、备份目录和 Kuscia 检查项。
5. 提交一条唯一标题的服务支持工单，刷新后确认工单出现在列表中。

预期：帮助内容可展开；诊断给出每个检查项的状态和详情；工单保存后可追溯。

### 8.2 演示录屏/截图清单

按以下顺序录制或截图，并记录测试日期、分支 SHA、测试人和结果：

1. 登录及六个数据沙箱菜单。
2. 项目创建、数据资源浏览和字段授权。
3. 沙箱创建、资源配额展示和状态监控。
4. 模型提交、两级审批、发布及审批记录。
5. 统一日志组合检索和 CSV 导出。
6. API 凭证、Webhook、OIDC 配置页面。
7. 备份、诊断、帮助和工单页面。

## 9. X-07 个人隔离部署与开发测试

### 9.1 隔离性检查

执行：

```bash
./develop.sh status --name xzh --branch develop/xzh
docker ps --filter name=data-sandbox-dev-xzh
```

预期：仅出现 `data-sandbox-dev-xzh-kuscia` 与
`data-sandbox-dev-xzh-secretpad` 私有容器；控制台端口为 `19088`，Kuscia 端口为
`19080` 至 `19084`；不得操作 `root-kuscia-autonomy-alice`、
`root-kuscia-autonomy-secretpad-alice` 或 Bob 容器。

### 9.2 构建模式检查

1. 修改前端或后端代码后，执行不带 `--skip-build` 的 `develop.sh up`。
2. 检查 `.dev-runtime/xzh/build-manifest.txt`。
3. 测试通过后提交和推送两个子仓库。
4. 需要发布候选校验时，执行：

```bash
./develop.sh up --name xzh --branch develop/xzh --pushed-only
```

预期：日常模式的清单包含 `source_mode=working-tree`；严格模式只接受工作区干净且与
上游一致的代码。构建过程不得覆盖开发者已有未提交源码修改。

### 9.3 当前运行时限制

当前私有 Kuscia 已启动，但尚未注册 Jupyter/Python 的 AppImage。环境镜像表中显示
“未配置 Kuscia AppImage”时，点击启动会返回明确错误；这证明错误反馈有效，但不代表
真实沙箱已经可运行。不要填入虚构的 AppImage 名称。

## 10. 测试记录模板

每次测试在下表追加一行：

| 日期 | 分支/SHA | 测试项 | 结果 | 证据 | 缺陷或限制 |
| --- | --- | --- | --- | --- | --- |
| 2026-08-17 | `develop/xzh` | 私有环境启动、六个菜单加载 | 通过/失败 | 截图或日志路径 | 说明 |

“通过”只表示对应步骤的前端交互和已接入接口正常；涉及真实运行时、数据安全策略、
性能或采购方平台联调的条目，必须有可重复的后端验证证据后才能标记为验收通过。
